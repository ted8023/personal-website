---
import { site } from '../data/site';
---
<footer class="site-foot">
  <div class="foot-socials">
    {site.contact.map((c) => (
      <a href={c.href} data-hot>{c.label}</a>
    ))}
  </div>
  <p class="foot-copy">© {new Date().getFullYear()} 阿秋 · 试试 ↑↑↓↓←→←→ B A</p>
</footer>
